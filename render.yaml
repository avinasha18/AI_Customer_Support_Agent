services:
  # Backend service
  - type: web
    name: virallens-chatbot-backend
    env: docker
    dockerfilePath: ./server/Dockerfile
    dockerContext: ./server
    plan: starter
    region: oregon
    branch: main
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: MONGO_URI
        fromDatabase:
          name: mongodb-atlas
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: OPENROUTER_API_KEY
        value: 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
      - key: CORS_ORIGIN
        value: https://support-virallens.vercel.app

  # Frontend service
  - type: web
    name: virallens-chatbot-frontend
    env: docker
    dockerfilePath: ./client/Dockerfile
    dockerContext: ./client
    plan: starter
    region: oregon
    branch: main
    healthCheckPath: /
    envVars:
      - key: VITE_API_URL
        value: https://ai-customer-support-agent-1.onrender.com/api
    buildCommand: echo "Building frontend..."

databases:
  - name: mongodb-atlas
    plan: starter
    region: oregon
